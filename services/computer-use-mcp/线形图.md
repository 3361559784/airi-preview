这是一份**极其详细的模块级别架构与数据流向表**。我把黑盒拆开了，重点呈现了 **QueryEngine 的内部 ReAct 大循环时序**、**Tool Router 的阶梯退避策略** 以及 **安全网在中间件层的拦截细节**。

这份图能帮助你理解任何由于“大模型乱改文件”或“死循环”导致的问题，到底在哪里被强行踩了刹车：

```mermaid
graph TD
    classDef engine fill:#1f2937,stroke:#6366f1,stroke-width:2px,color:#fff;
    classDef router fill:#374151,stroke:#10b981,stroke-width:2px,color:#fff;
    classDef failGuard fill:#7f1d1d,stroke:#ef4444,stroke-width:2px,color:#fff;
    classDef state fill:#4b5563,stroke:#eab308,stroke-width:1px,color:#fff;

    %% 核心调起
    Start([1. 收到 Agentic Run 请求]) --> Boot[构造 QueryEngine 实例<br/>加载 Workspace & Session]

    subgraph QueryEngine [QueryEngine 调度引流器 - engine.ts]
        class QueryEngine engine

        Boot --> PreFlight[Pre-flight 检查]
        PreFlight --> PromptBuilder[System Prompt 注入层<br/>结合 Discover/Plan/Edit 策略与上下文]
        
        %% ReAct大循环
        PromptBuilder --> LLMRequest(调用大语言模型)
        LLMRequest --> LLMStream[处理 LLM Streaming Chunk<br/>通过 SSE 分发给客户端]
        LLMStream -.返回.-> ActionCheck{评估模型输出}
        
        ActionCheck --只有文本回答--> PhaseStop([2A. 对话返回，此轮阻断结束])
        ActionCheck --生成 Tool Calls--> GuardCheck{Budget & History 验证}
        
        %% 核心守卫截断网
        GuardCheck --> TokenizerCheck[<b>Tokenizer</b><br/>检查 Token 是否超越上限]
        TokenizerCheck --> CompressionCheck[<b>Context Compact</b><br/>如达 70% 阈值, 启动会话抽稀压缩]
        CompressionCheck --> BudgetCheck[<b>Budget Guard</b><br/>验证 maxTurns & maxToolCalls]
        BudgetCheck -.如果违规.-> AbortRun([2B. 预算或限制耗尽, 抛出异常终结])
    end

    %% 工具下发与执行
    BudgetCheck --放行--> RouteEntry(进入工具管道调度)

    subgraph ToolRouterNode [Tool Router 动态映射与兜底区 - tool-router.ts]
        class ToolRouterNode router
        
        RouteEntry --> SwitchRouter{提取 Function Name}
        
        %% 读系列
        SwitchRouter --read_file / search / list--> ReadLogic[提取缓存/执行读取]
        %% 危险终端类
        SwitchRouter --bash--> BashInterceptor{<b>Bash Guard 禁令墙</b><br/>rm/sed/tee等劫持}
        BashInterceptor --发现危险指令--> RejectBash[强制覆盖指令，抛出引导使用标准edit的Error]:::failGuard
        BashInterceptor --纯查询--> ExecuteBash[放行并抛给底层的 PTY]
        %% 编辑系列
        SwitchRouter --edit_file / multi_edit--> EditPipeline[<b>多阶降级重试编辑管线 </b>]
    end

    %% 编辑管线深度展开
    subgraph EditFallback [Edit File 的鲁棒兜底瀑布流]
         EditPipeline --> EM_Exact[1. 完美匹配 <br/> Exact Match]
         EM_Exact -.失败.-> EM_WS[2. 空白字符规整 <br/> Whitespace Normalized]
         EM_WS -.失败.-> EM_Indent[3. 缩进重组 <br/> Indent Normalized]
         EM_Indent -.失败.-> EM_Fuzzy[4. 字符串模糊滑动 <br/> Fuzzy Window]
         EM_Fuzzy -.失败.-> EditError[报错并把当前文件的 Preview 丢给 LLM]:::failGuard
         EM_Exact -.成功.-> FileWrite[(保存进 Workspace)]
         EM_WS -.成功.-> FileWrite
         EM_Indent -.成功.-> FileWrite
         EM_Fuzzy -.成功.-> FileWrite
    end

    %% 结果反馈大回流
    FileWrite --> CaptureResult
    ExecuteBash --> CaptureResult
    ReadLogic --> CaptureResult
    RejectBash -.安全拦截信息.-> CaptureResult
    EditError -.诊断信息与文件上下文.-> CaptureResult

    CaptureResult[汇总执行结果] --> TruncatorGuard{<b>Tool Result Truncator</b><br/>结果体积是否超 50K?}
    
    TruncatorGuard --是--> SliceData[硬性切片：<br/>保留 70% 原头 + 20% 结尾片段]:::failGuard
    TruncatorGuard --否--> DirectReturn[保留原始数据]
    
    SliceData --> UpdateState
    DirectReturn --> UpdateState

    subgraph StateManagement [运行时持久态与反哺记忆]
        class StateManagement state
        UpdateState[把工具运行结果与最新文件状态打包<br/>更新到 SessionState]
        UpdateState --> WriteSession(每 5 RTT 落盘一次/tmp状态备份)
    end
    
    WriteSession --> LLMRequest
```

### 进一步研读这套图表的 3 个核心细节：

1. **ReAct 引擎的安全钳制（左上角 `QueryEngine` 区）**
   - 这里的设计摒弃了将 Agent 全权托付给 LLM 自由跳舞的做法。**在向工具路由器派发前，每一轮动作都横切了一道 `Tokenizer -> Context Compact -> Budget Guard` 的安检长廊**。 
   - 由于大语言模型在长期修改文件后易发生 “上下文 Token 大爆炸” 以致于陷入死机或拖死 API Budget，这里的 `Context Compact` 会在预算水位抵达 70% 时暴力剔除中间记忆进行摘要压缩。

2. **为什么不让模型直接敲 Bash 编辑代码？（中间 `Tool Router` 区）**
   - 很多早期的 Coding Agent 会赋予大模型完全的终端读写权限。由于模型缺乏精准光标认知，极其容易写出如 `echo > file` 或胡乱 `sed` 而引发灾难。
   - `Tool Router` 在收到 `bash` 命令时，中间设有一个 `BashGuard` 探针屏障：只要正则解析到危险修改模式，**请求根本下不到终端**，引擎会伪造一段真实的终端报错信息（"Error: Do not use bash for edits. Please use edit_file"）骗大模型利用专门的编辑器链进行修改。

3. **阶梯降级编辑瀑布流（右侧 `Edit File` 区）**
   - 当大模型想利用 `edit_file` 修改局部代码块时，大模型经常会由于幻觉，在缩进数量或空行数上写出一部分差异。如果要求严格全匹配（Exact），往往会连续发生 "target string not found" 死链。
   - 这条编辑管线像个漏斗，一级比一级贪婪。如果完美匹配不到，会逐级清除两端的空白字符，如果不成则重新排列首行缩进（Indent Normalized）。当模型因为代码产生过多缩进而卡住时，这个重试引擎总能兜底找到正确编辑缝隙。如果全挂了，则把原文件相关的 Chunk 截出来吐回给大模型。


